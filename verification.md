# 検証手順と結果

確認日: 2026-09-10。成功した項目と、実アカウントの承認が必要な未確認項目を分けて記録します。

## 結果

| 範囲 | 結果 |
|---|---|
| C++状態・解析テスト | 28件成功。消灯、復帰タッチ、BOOTのチャタリング、時刻の巻き戻り、接続/DHCP待ち、使用量の欠損・不正値を確認。 |
| Pythonテスト | リリースノート抽出の4件成功。スクリプトの構文確認も成功。 |
| 初期設定Web画面 | 専用Chromeで日本語/英語、Wi-Fi入力、ログイン開始、コード表示、消灯設定保存、入力エラーを検証。320px幅とデスクトップ幅で横はみ出しなし。 |
| ESP32ビルド | Arduino 3.3.11 / ESP-IDF 5.5.5で、通常版と診断版のリンク・バイナリ生成成功。 |
| LCD表示 | 実機のLCD RAMを読み戻した320×240画像で、使用量と設定画面、日本語、ボタン配置を確認。 |
| ちらつき対策 | 8-bit画面バッファを確保し、完成した画面を一度だけ転送する経路を実機で確認。通信中も確保状態を維持。観測した空きヒープ最小値は48,912バイト。 |
| BOOT反転 | USB自動書き込み回路から実GPIO0を操作し、0度/180度の切り替えと押しっぱなし時の一回動作を確認。消灯中の復帰・反転、再起動後の向きの保存も確認。 |
| 消灯設定 | LCDの9選択肢を入力経路から操作して保存を確認。ボタン間の余白では選択が変わらない。 |
| 消灯・復帰 | 消灯中にも実際の使用量更新が進むこと、最初のタッチとその保持中は設定を変更しないこと、離して次に押すと操作できることを確認。 |
| Wi-Fi・再起動 | 一時的な2.4GHz WPA2 APへ接続し、ESP32からHTTPSで実際の使用量を取得。再起動後の再接続と再取得も確認。 |
| 設定移行 | version 1の設定を読み込み、version 2へ移行。SSID・パスワードを維持したまま消灯時間・反転状態を保存できることを照合。 |
| 設定サーバーの公開範囲 | STA側LANから設定サーバーへアクセスできないことを実機で確認。AP側の画面操作はブラウザモックで検証。 |
| 元ファームウェア復元 | 全4MBの書き戻し後、verify-flashで一致を確認。元アプリの起動通知も確認。 |
| ホスト環境の復旧 | 検証APを停止し、ホットスポット設定・Wi-Fi無線状態を復元。指定のデバッグFirefoxを終了し、提供されたHome設定修復スクリプトを実行。 |

物理タッチパネルの四隅を人の指で押す校正確認は未実施です。自動試験では本番と同じタッチ処理へ入力を渡し、状態遷移とボタン判定を検証しています。

## 認証の確認範囲

デバイス認証コードの発行はHTTP 200で確認しました。ただし検証用FirefoxでCloudflareの確認画面が継続したため、初回の承認、認可コード交換、実際の更新トークンによる更新成功は未検証です。成功したログインとして扱いません。

実機の通信試験では、許可されたブラウザセッションのアクセストークンを一時的な診断用NVSへ投入しました。PC側の更新トークンは共有せず、使用量取得・保存・再起動の経路を確認しています。この試験は、デバイス認証全体や更新トークン更新の代替検証ではありません。

実アカウントから返った通常枠は週間のみで、5時間枠は未提供でした。解析はフィールド順ではなく期間秒数で行い、別モデルの追加枠を代用しません。

残る確認は、通常のブラウザでデバイスコードを承認し、ESP32での認可コード交換、更新トークン保存、期限前更新、再起動後の継続利用を確認することです。APへのスマートフォン接続、設定送信、APの15分停止も実環境での追加確認が必要です。

## 実機試験で見つけた問題と対策

- 起動直後にシリアル要求を一度だけ送る試験では、初期化前に要求が失われた。診断開始を早め、試験側は起動を待って再照会する。
- Wi-Fiに関連付け済みでもIP未取得の場合がある。30秒ごとに切断すると取得を中断するため、DHCPには120秒の猶予を設けた。
- 検証APで再起動後にDHCPのSELECTING状態が続いた。IP未取得時の応答をRFC 2131 §4.1のブロードキャストで要求する比較試験では、同じAPへの再接続が成功した。MAC変更、固定IP、IP競合検証の無効化は使用していない。
- 全画面を消してから描く方式では操作時にちらついた。Wi-Fi開始前に約76.8KBのSpriteを確保し、描画完了後だけLCDへ転送する。

## 再検証コマンド

前提となるツールの導入は [how-to-update.md](how-to-update.md) を参照してください。

```powershell
$env:PYTHONUTF8 = "1"
pio pkg install -e native
python scripts/test-native.py
python -m unittest discover -s test -p 'test_*.py'
python -m compileall -q scripts
python scripts/test-setup-ui.py
python scripts/embed-web.py
git diff --exit-code -- include/setup-page.h
pio run -e cyd
```

LinuxのCIでは `pio test -e native` を使用します。GitHub上のCI実行・リリース公開は、この作業では行っていません。

診断版は `pio run -e cyd-diagnostics` でビルドします。USB 921600bpsへ1行のJSONで `{"cmd":"state"}`、`{"cmd":"touch","x":155,"y":15,"pressed":true}`、`{"cmd":"boot","pressed":true}` などを送り、各応答後に次の操作を行います。押下後は `pressed:false` で離し、`physical` で合成入力を解除します。`screen` の応答はJSONヘッダー、`bytes` バイトのRGB画像、改行の順です。診断版は配布しません。

## バックアップと復旧の証跡

この実機のバックアップはGit管理外の `.local/original-firmware.bin`（4,194,304バイト）です。

```text
SHA-256: 023e3f264c912109865df92fa2949571f854692124c0014a58f2848f82a3d132
```

復元手順は [how-to-update.md](how-to-update.md) の `scripts/flash-backup.py restore` を使います。検証用イメージに含めた認証情報を製品やリリースへ混入させず、元フラッシュを検証してから通常起動します。

## 依存関係の確認

Espressifの公開アドバイザリを確認し、WebServerの入力長・ヘッダー検証修正を含むArduino 3.3.11を採用しました。導入済みソースでも修正を確認しています。開発ツールはPlatformIO 6.2.0とStarlette 1.6.0へ更新し、専用venvの43パッケージへのOSV照会は該当なしでした。ArduinoJsonとLovyanGFXのリポジトリ公開アドバイザリにも該当報告はありませんでした。未報告の問題がないことを保証するものではありません。
