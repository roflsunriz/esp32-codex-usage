# 変更履歴

このプロジェクトの利用者向け変更を、[Keep a Changelog](https://keepachangelog.com/ja-JP/1.1.0/) の形式で記録します。

## [Unreleased]

## [0.1.0-beta.1] - 2026-09-10

初回ベータリリースです。初回のデバイス認証承認と更新トークンによる更新成功は、Cloudflare確認が必要なため未検証です。実機では使用量取得、消灯・復帰、BOOT反転、設定保存、再起動後のWi-Fi再接続を確認しました。

`firmware.factory.bin` は初回導入用で、0x0から書き込むと設定領域も上書きします。必ず元ファームウェアをバックアップしてください。5時間枠がAPIから提供されない場合は `--` と表示します。

### Added

- ESP32-2432S028R向けLovyanGFX表示、XPT2046タッチ、320×240横向きUIを追加。
- 使用量、設定、接続の3タブと、15秒から2時間まで9種類の自動消灯を追加。
- BOOTボタン単押しで180度回転を切り替え、NVSへ保存する設定移行を追加。
- 消灯後の最初のタッチを復帰専用にする状態機械と、確認済みNVS設定だけを表示側へ反映する処理を追加。
- 操作のたびに画面がちらつかないよう、8-bit画面バッファで完成したフレームを転送する描画を追加。
- IPアドレス取得の途中で接続を切らないよう、Wi-Fi接続とDHCPの待ち時間を分ける再接続監視を追加。
- 再起動後もIPアドレスを取得できるよう、IP未取得時のDHCP応答をブロードキャストで要求する互換処理を追加。
- Wi-Fi初期設定AP、2.4 GHzルーター設定用Web画面、日本語・英語切り替え、APの15分自動停止と手動再開を追加。
- 公式デバイス認証、認証コードのポーリング、トークン更新、使用量取得、失敗時の再試行を追加。
- refresh tokenをNVSのJSON blobへ保存し、4,000バイト超のトークンにも対応。
- production用 `cyd` と、合成タッチ・仮想時刻・画面状態取得用の `cyd-diagnostics` プロファイルを追加。
- フルフラッシュのバックアップ・照合・復元を行う `scripts/flash-backup.py` を追加。

### Changed

- WebServerの既知の修正を含むpioarduino platform 55.03.311（Arduino core 3.3.11、ESP-IDF 5.5.5）へ移行した。
- 使用量ウィンドウはフィールド順ではなく `limit_window_seconds` で分類し、5時間枠が未提供の場合は代替クォータを表示しない。

### Security

- TLSルート証明書を同梱し、認証・使用量通信で証明書検証を行う。
- 開発ツールの既知の脆弱性を避けるため、PlatformIO 6.2.0と修正済みStarlette 1.6.0を固定した。
- APIキーを保存せず、トークン・NVS・フラッシュバックアップを機密情報として扱う手順を追加した。
- Secure Boot、Flash Encryption、eFuseへの強制操作を更新・復元手順から除外した。
