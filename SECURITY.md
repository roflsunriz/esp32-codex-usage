# セキュリティ方針

## 対象

このプロジェクトは、ESP32へChatGPT/Codexの使用量を表示する個人用モニターです。認証トークン、Wi-Fi認証情報、使用量、フラッシュバックアップは機密データとして扱います。

## 報告

再現手順や影響範囲を含む報告は、公開Issueへ秘密情報を貼らず、リポジトリ管理者へ非公開で連絡してください。トークン、Cookie、フラッシュダンプ、Wi-Fiパスワード、個人アカウントIDを送らないでください。ログを共有する場合は該当行を削除またはマスクします。

## 保存と通信

- HTTPSは同梱ルート証明書で検証します。証明書検証を無効化する変更は受け入れません。
- APIキーは使わず、公式のデバイス認証で取得したトークンを使います。
- access/refresh tokenはESP32のNVSへ保存します。
- NVSはフラッシュ暗号化を有効にしていない個体では物理アクセスから保護されません。製品化時はESP32のセキュリティ機能と鍵管理を別途設計します。
- 初期設定APのパスワードはランダムに生成し、LCD上でのみ表示します。APを不要な時間に開いたままにしません。
- HTTP初期設定画面はローカル設定用です。インターネットへポート転送しません。

## バックアップと復旧

フルフラッシュにはNVSが含まれる可能性があります。`.local/original-firmware.bin` をGitへ追加せず、アクセス制限された場所で保管します。実機検証後は元イメージを書き戻し、esptoolの検証と起動確認を行います。Secure BootやFlash Encryptionを `--force` で回避したり、eFuseを書き換えたりしません。

## 依存関係とprivate endpoint

使用量取得先 `https://chatgpt.com/backend-api/wham/usage` は公開API契約ではありません。応答形式や認証条件が変わったときは、推測で代替エンドポイントを追加せず、失敗を明示して更新を止めます。認証ソースを変更する場合は一次資料を確認し、依存バージョンと検証結果を文書化します。
